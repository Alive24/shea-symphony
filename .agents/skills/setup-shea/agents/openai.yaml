interface:
  display_name: "Setup Shea"
  short_description: "Onboard and reconcile a Shea repository"
  default_prompt: "Use $setup-shea to verify or install the matching stable Shea App, reconcile this repository from the same immutable release, and finish with a no-claim readiness report."
