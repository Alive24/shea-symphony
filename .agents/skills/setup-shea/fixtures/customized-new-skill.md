# Customized Backlog Or Improve Skill

## Observed input

- A target's vendored `shea-symphony-improve/references/architecture-lens.md`
  differs from the staged stable-release bytes.
- Legacy Dream and Reflect removal may be planned in the same run.

## Expected plan

- Classify the customized Improve reference as `conflict_keep` by default and
  show its focused difference.
- Keep legacy removal confirmation independent from any replacement choice for
  Backlog or Improve.

## Expected result

- Preserve the customized new Skill byte-for-byte unless the operator
  explicitly chooses `conflict_replace` or a reviewed manual merge.
- Never silently overwrite target-owned Backlog or Improve files.
