interface:
  display_name: "Shea Symphony Manual Review"
  short_description: "Trigger one workflow-backed external review"
  default_prompt: "Use $shea-symphony-manual-review to trigger the configured external Review backend for one named Shea Symphony issue and read back its result."
