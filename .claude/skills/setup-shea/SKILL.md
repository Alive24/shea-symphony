---
name: setup-shea
description: Onboard or reconcile Shea Symphony in a target repository from one immutable stable release. Use when setup is missing or incomplete, or when Doctor routes a runtime-profile or repository-contract problem back to setup. Do not use to execute a lane or to overwrite an onboarded repository's own customizations.
---

# Setup Shea (Claude Code entry point)

`.agents/skills/setup-shea/SKILL.md` is the authoritative body of this Skill, shared
by every supported harness. Read it now and follow it verbatim, including every
reference it resolves. This file only makes that body discoverable; it carries
no policy, permission or lane authority. Use `docs/README.md` to route to the narrowest authoritative repository context, and the `.agents/` body wins on any
disagreement.
