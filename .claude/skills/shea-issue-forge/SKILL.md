---
name: shea-issue-forge
description: Turn the user's intent into a quality-gated executable Shea Issue contract, then create, promote or rework it after explicit confirmation. Use when the user wants a new Issue, a Backlog seed promoted, or a Human Review rework scope written. Do not use to execute the resulting Issue or to record execution progress.
---

# Shea Issue Forge (Claude Code entry point)

`.agents/skills/shea-issue-forge/SKILL.md` is the authoritative body of this Skill, shared
by every supported harness. Read it now and follow it verbatim, including every
reference it resolves. This file only makes that body discoverable; it carries
no policy, permission or lane authority. Use `docs/README.md` to route to the narrowest authoritative repository context, and the `.agents/` body wins on any
disagreement.
